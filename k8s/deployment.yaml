apiVersion: apps/v1
kind: Deployment
metadata:
  name: bike-showroom
  labels:
    app: bike-showroom
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bike-showroom
  template:
    metadata:
      labels:
        app: bike-showroom
    spec:
      containers:
        - name: bike-showroom
          image: bike-showroom:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
